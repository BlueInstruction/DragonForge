diff --git a/src/freedreno/vulkan/tu_cmd_buffer.cc b/src/freedreno/vulkan/tu_cmd_buffer.cc
--- a/src/freedreno/vulkan/tu_cmd_buffer.cc
+++ b/src/freedreno/vulkan/tu_cmd_buffer.cc
@@ -1,3 +1,4 @@
+/* Dragon: Phoenix Wings Boost - Performance optimizations */
 
 /* Performance boost through optimized rendering paths */
 
@@ -150,10 +151,18 @@ tu_cmd_buffer_select_render_mode(struct tu_cmd_buffer *cmd)
    /* Phoenix: Aggressive binning optimization */
+   if (cmd->state.pass->subpass_count == 1) {
+       /* Single subpass - use direct rendering for speed */
+       cmd->state.render_mode = TU_RENDER_MODE_DIRECT;
+   }
+   
+   /* Phoenix: Reduce unnecessary flushes */
+   cmd->state.cache.conservative_flush = false;
 
 diff --git a/src/freedreno/vulkan/tu_pipeline.cc b/src/freedreno/vulkan/tu_pipeline.cc
--- a/src/freedreno/vulkan/tu_pipeline.cc
+++ b/src/freedreno/vulkan/tu_pipeline.cc
@@ -1,3 +1,4 @@
+/* Dragon: Phoenix Wings Boost - Pipeline optimizations */
 
@@ -80,6 +81,14 @@ tu_pipeline_create(struct tu_device *dev)
    /* Phoenix: Optimize pipeline creation */
+   pipeline->lrz.enable = true;
+   pipeline->lrz.write = true;
+   
+   /* Enable fast clear */
+   pipeline->fast_clear = true;
+   
+   /* Optimize vertex fetch */
+   pipeline->vertex_fetch_optimize = true;
 
 diff --git a/src/freedreno/ir3/ir3_compiler.c b/src/freedreno/ir3/ir3_compiler.c
--- a/src/freedreno/ir3/ir3_compiler.c
+++ b/src/freedreno/ir3/ir3_compiler.c
@@ -1,3 +1,4 @@
+/* Dragon: Phoenix Wings Boost - Shader compiler optimizations */
 
@@ -100,6 +101,12 @@ ir3_compiler_create(struct fd_device *dev)
    /* Phoenix: Aggressive shader optimizations */
+   compiler->optimization_level = 3;
+   
+   /* Enable instruction scheduling */
+   compiler->sched_enabled = true;
+   
+   /* Enable register allocation optimization */
+   compiler->ra_optimize = true;
